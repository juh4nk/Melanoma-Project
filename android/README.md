# Android App (Java)

This folder will contain the Android demo application.

Planned contents:
- on-device model inference using an exported model (e.g., .tflite)
- simple UI for image input
- privacy-by-default handling (discard input immediately; no storage)

Training code does not live here.
