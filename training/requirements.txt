# requirements.txt
# =================
# Python dependencies for the melanoma detection training pipeline.
# Install with: pip install -r requirements.txt
# Tested with Python 3.10+

# Core ML framework
tensorflow>=2.13.0        # model building, training, TFLite export

# Data handling
numpy>=1.24.0
pandas>=2.0.0             # CSV parsing for ISIC label files

# Evaluation & metrics
scikit-learn>=1.3.0       # confusion matrix, ROC-AUC, classification report

# Visualization
matplotlib>=3.7.0         # confusion matrix and ROC curve plots
seaborn>=0.12.0           # styled confusion matrix heatmap

# Config (for future YAML config support)
PyYAML>=6.0